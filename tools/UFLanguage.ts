/**
 * Returns a translated version of text.
 *
 * @param {string} aText
 *   Original text (will be used by the scanner to build text files)
 * @param {string} [anId]
 *   Optional id, when missing aText is used an id.
 *
 * @returns {string} The text for the current selected language.
 */
export function tt(aText: string, anId?: string): string {
  return aText;
}